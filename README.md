## simple isomorphic react 
   a simple react todo app with client and server rendering

### Install packages:

```js
// Windows
npm install

// OSX
$ sudo npm install
```

### To run it locally:
```
node app
```

